# See following for more information: http://www.infinitered.com/blog/?p=19

# Path ------------------------------------------------------------
export PATH=/usr/local/bin:/usr/bin:$PATH  # OS-X Specific, with MacPorts installed
export PATH=$HOME/.local/bin:$PATH
export PATH=/opt/homebrew/bin:$PATH

# export LDFLAGS="-L/opt/homebrew/opt/llvm/lib"
# export CPPFLAGS="-I/opt/homebrew/opt/llvm/include"
# export PATH="/opt/homebrew/opt/llvm/bin:$PATH"

export PATH=$HOME/.config/emacs/bin:$PATH

# export DYLD_LIBRARY_PATH="/opt/homebrew/lib"

# export NVM_DIR=$HOME/.nvm
# [ -s $NVM_DIR/nvm.sh ] && . $NVM_DIR/nvm.sh  # This loads nvm

#
export ICLOUD="$HOME/Library/Mobile Documents/com~apple~CloudDocs"
export WORKSPACE=$HOME/Workspace

# Secrets — real values live in ~/.env (chmod 600, never tracked).
[ -f "$HOME/.env" ] && source "$HOME/.env"

# export ANTHROPIC_AUTH_TOKEN=ollama
# export ANTHROPIC_BASE_URL=http://localhost:11434

# jax
export ENABLE_PJRT_COMPATIBILITY=1

eval "$(/opt/homebrew/bin/brew shellenv)"

# source ~/.profile
source ~/.zshrc

# Git identity lives in ~/.gitconfig [user]. No env-var override needed.

# # pyenv setup
# export PYENV_ROOT="$HOME/.pyenv"
# export PATH="$PYENV_ROOT/bin:$PATH"
# export PATH="$PYENV_ROOT/shims:${PATH}"
# eval "$(pyenv init -)"
# eval "$(pyenv virtualenv-init -)"
#
# export PYENV_VIRTUALENV_DISABLE_PROMPT=1
# export BASE_PROMPT=$PS1
# function updatePrompt {
#     if [[ "$(pyenv version-name)" != "system" ]]; then
#         # the next line should be double quote; single quote would not work for me
#         export PS1="($(pyenv version-name)) "$BASE_PROMPT
#     else
#         export PS1=$BASE_PROMPT
#     fi
# }
# export PROMPT_COMMAND='updatePrompt'
# precmd() { eval '$PROMPT_COMMAND' } # this line is necessary for zsh

# Added by OrbStack: command-line tools and integration
# source ~/.orbstack/shell/init.zsh 2>/dev/null || :
# eval "$(/opt/homebrew/bin/brew shellenv)"
